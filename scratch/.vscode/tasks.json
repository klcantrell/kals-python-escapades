{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pyright",
            "type": "shell",
            "command": "npm run pyright && source ${fileDirname}/.venv/scratch_env/bin/activate && flake8 ${file}"
        }
    ]
}
